scripted_gui = {
	ASF_Dual_Leader_Pol_GUI = {
		window_name = "ASF_Dual_Leader_Pol_GUI"
		context_type = player_context
		parent_window_token = politics_tab

		triggers = {
			dual_leader_portrait_click_enabled = {
				set_temp_variable = { temp_portrait = ASF_second_leader:character_token }
			}
		}

		effects = {
			fetch_dual_leader_description = {
				meta_effect = {
					text = {
						custom_effect_tooltip = [CHAR]_desc
					}
					CHAR = "[?ASF_second_leader:character_token.GetTokenKey]"
				}
			}
		}

		properties = {
			dual_leader_portrait = {
				image = "GFX_portrait_[?temp_portrait.GetTokenKey]"
			}
		}
	}

	ASF_Dual_Leader_Diplo_GUI = {
		window_name = "ASF_Dual_Leader_Diplo_GUI"
		context_type = selected_country_context
		parent_window_token = selected_country_view_diplomacy

		triggers = {
			dual_leader_portrait_click_enabled = {
				set_temp_variable = { temp_portrait = ASF_second_leader:character_token }
			}
		}

		effects = {
			fetch_dual_leader_description = {
				meta_effect = {
					text = {
						custom_effect_tooltip = [CHAR]_desc
					}
					CHAR = "[?ASF_second_leader:character_token.GetTokenKey]"
				}
			}
		}

		properties = {
			dual_leader_portrait = {
				image = "GFX_portrait_[?temp_portrait.GetTokenKey]"
			}
		}
	}
}